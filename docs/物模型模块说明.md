**目录**  

- [1. 功能介绍](#1-功能介绍)
- [2. 使用流程](#2-使用流程)
- [3. 接口参考](#3-接口参考)
  - [接口列表](#接口列表)
  - [接口描述](#接口描述)
    - [iv\_dm\_init](#iv_dm_init)
    - [iv\_dm\_exit](#iv_dm_exit)
    - [iv\_dm\_event\_report](#iv_dm_event_report)
    - [iv\_dm\_property\_report](#iv_dm_property_report)
    - [iv\_dm\_property\_sync](#iv_dm_property_sync)
    - [iv\_dm\_init\_sync\_status](#iv_dm_init_sync_status)
    - [ivm\_env\_init](#ivm_env_init)
    - [ivm\_lock](#ivm_lock)
    - [ivm\_unlock](#ivm_unlock)
    - [iv\_dm\_env\_init\_cb](#iv_dm_env_init_cb)
    - [ivm\_property\_report\_cb](#ivm_property_report_cb)
    - [iv\_msg\_send\_notice](#iv_msg_send_notice)
    - [iv\_msg\_send\_notice\_ext](#iv_msg_send_notice_ext)
- [4. 数据结构](#4-数据结构)
  - [数据结构列表](#数据结构列表)
  - [数据结构描述](#数据结构描述)
    - [iv\_dm\_report\_result\_e](#iv_dm_report_result_e)
    - [iv\_dm\_init\_parm\_s](#iv_dm_init_parm_s)
- [5. 注意事项](#5-注意事项)
- [6.物模型的创建和调试](#6物模型的创建和调试)
  - [1.创建产品物模型](#1创建产品物模型)
  - [2.物模型添加属性，事件，行为功能的示例](#2物模型添加属性事件行为功能的示例)
    - [2.1下图是新增物模型属性的示例，](#21下图是新增物模型属性的示例)
    - [2.2下图是新增物模型事件的示例，参数数量可以有多个，参数类型也可以任意选择。](#22下图是新增物模型事件的示例参数数量可以有多个参数类型也可以任意选择)
    - [2.3下图是新增物模型行为的示例，](#23下图是新增物模型行为的示例)
  - [2.4导入已有产品的物模型](#24导入已有产品的物模型)
  - [2.5导出物模型](#25导出物模型)
  - [2.6生成物模型的C代码](#26生成物模型的c代码)
  - [3.物模型的调试](#3物模型的调试)
    - [3.1物模型读写属性的调试](#31物模型读写属性的调试)
    - [3.2物模型行为的调试](#32物模型行为的调试)
    - [3.3物模型事件的调试](#33物模型事件的调试)
- [7. 示例代码](#7-示例代码)
  - [1. 物模型初始化](#1-物模型初始化)
  - [2. 物模型去初始化](#2-物模型去初始化)
  - [3. 属性和事件的上报](#3-属性和事件的上报)

--------

# 1. 功能介绍

该模块是 IoT Video 的必选模块，需要第二个初始化。

物模型部分分为

- 属性(property)
  属性对设备能力的描述，通过对属性的修改即可实现对设备的控制,其又可分为：读写属性 (ProWritable) 和只读属性 (ProReadonly) 
- 行为(action)
  控制设备执行特定的行为，并将执行的结果返回。行为与属性的区别，概念上行为是数据和方法的组合，行为有执行结果的返回。属性只有数据，修改属性后设备侧是否执行成功很难在属性本身体现。
- 事件(event);
  设备发生特定情况，譬如灯的开关状态发生了变化，上报事件。应用侧收到事件后按预设逻辑推送事件。

物模型由用户声明好后导出相应的 json 文件, 使用工程目录下的 `tool/codegen.py` 脚本生成代码，执行如下命令：
`./codegen.py -c <json文件>`

最终生成如下代码：

```
iv_usrex.c:用户物模型实现源文件,实现物模型初始化相关的代码,开发者不能修改这个文件
iv_usrex_func.c:用户物模型功能实现函数,实现相应 ProWritable 和 action 的回调
iv_usrex.h:用户物模型定义头文件,定义相关数据结构,开发者不能修改这个文件
```

在编译时编入工程，根据用户功能需要，在设备初始化阶段注册不同物模型处理函数，SDK 在相应模型触发时回调，用户亦可调用物模型接口主动上传物模型数据，对于设备来说，支持 ProWritable 类模型数据接收和发送, 支持 ProReadonly 和 Event 类模型数据发送， 支持 Action 模型数据接收。

# 2. 使用流程

注册下发消息回调 > 物模型初始化 > 发送物模型消息 > 物模型去初始化

# 3. 接口参考

## 接口列表

**该功能模块提供以下接口**  

- iv_dm_init 物模型初始化接口;
- iv_dm_exit 物模型去初始化接口;
- iv_dm_event_report 事件上报接口;
- iv_dm_property_report 属性上报接口;
- iv_dm_property_sync 属性同步接口
- ivm_env_init 物模型环境初始化,由脚本生成;
- ivm_lock 物模型上报加锁;
- ivm_unlock 物模型上报解锁;

**用户需注册以下回调函数**  

- (*iv_dm_env_init_cb)() 物模型初始化参数回调
- (*ivm_property_report_cb)() 属性上报结果回调

## 接口描述

### iv_dm_init

**功能描述**  
物模型初始化，进行物模型模块资源申请，设备启动时调用。

**函数原型**  

```
int iv_dm_init(const iv_dm_init_parm_s *pstInitParm);
```

**参数说明**  
| 参数名称    | 类型              | 描述       | 输入/输出 |
| ----------- | ----------------- | ---------- | --------- |
| pstInitParm | iv_dm_init_parm_s | 初始化参数 | 输入      |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

### iv_dm_exit

**功能描述**  
物模型去初始化，进行物模型模块资源释放，设备退出时调用。

**函数原型**  

```
int iv_dm_exit(void);
```

**参数说明**  
| 参数名称 | 类型 | 描述 | 输入/输出 |
| -------- | ---- | ---- | --------- |
| 无       | 无   | 无   | 无        |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

### iv_dm_event_report

**功能描述**  
上传用户自定义事件消息。

**函数原型**  

```
int iv_dm_event_report(const char *event_name);
```

**参数说明**  
| 参数名称   | 类型       | 描述   | 输入/输出 |
| ---------- | ---------- | ------ | --------- |
| event_name | const char | 事件名 | 输入      |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

### iv_dm_property_report

**功能描述**  
上报属性信息

**函数原型**  

```
int iv_dm_property_report(const char *key, ivm_property_report_cb cb, void *param);
```

**参数说明**  
| 参数名称 | 类型                   | 描述              | 输入/输出 |
| -------- | ---------------------- | ----------------- | --------- |
| key      | const char *           | 自定义的属性key值 | 输入      |
| cb       | ivm_property_report_cb | 上报结果通知      | 输入      |
| param    | void *                 | 自定义参数        | 输入      |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

**注意事项**  

* 物模型上报后会触发回调`cb`，告诉用户最终的上报结果
* 必须等待当前物模型上报成功，才能进行下一次上报，否则会返回错误
* 物模型上报的超时时间根据`system`模块中的`command_timeout`确定，
* 尽量控制上报属性的频率, 频繁的轮询上报属性会额外占用带宽，一般建议不超过每秒1次，如有高频上报需求请联系客服

### iv_dm_property_sync

**功能描述**  
属性同步接口

**函数原型**  

```
int iv_dm_property_sync(int timeout_ms)
```

**参数说明**  
| 参数名称   | 类型 | 描述                            | 输入/输出 |
| ---------- | ---- | ------------------------------- | --------- |
| timeout_ms | int  | 同步超时时间，单位:ms，建议1000 | 无        |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

**注意事项**  

* 该接口是阻塞接口, 只可以在单一线程中调用，禁止多线程调用
* 调用该接口返回`IV_ERR_NONE`后, 会将结果同步到物模型全局变量`g_ivm_objs`中, 可以直接读取
* 需要在设备处于上线状态时，调用该接口才能同步到正确的值，返回`IV_ERR_DM_REQUEST_BUSY`表示调用太频繁，本次同步失败
* 本接口的限制策略为100秒内调用10次，比如可以在1秒内调用10次，剩下99秒等待，也可以每10秒调用1次，该策略在保证限频的同时也可以满足短时间内突发的大量调用请求

### iv_dm_init_sync_status

**功能描述**  
获取上线后（或重连后）SDK内部首次同步物模型是否成功的状态

**函数原型**  

```
int iv_dm_init_sync_status(void)
```

**参数说明**  
| 参数名称 | 类型 | 描述 | 输入/输出 |
| -------- | ---- | ---- | --------- |
| -        | -    | -    | -         |

**返回值**  
| 返回值 | 描述                 |
| ------ | -------------------- |
| 0      | 同步失败，或正在同步 |
| 1      | 同步成功             |

**注意事项**  

* 初始化时SDK内部会获取最新物模型，当调用本接口兵返回成功后，直接读取 g_ivm_objs 即可，可以免去用户首次调用 iv_dm_property_sync 进行手动同步的操作

### ivm_env_init

**功能描述**  
物模型环境初始化，由物模型代码脚本生成，无需修改。

**函数原型**  

```
int ivm_env_init(void);
```

**参数说明**  
| 参数名称 | 类型 | 描述 | 输入/输出 |
| -------- | ---- | ---- | --------- |
| -        | -    | -    | -         |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

**注意事项**  
该函数定义在`iv_usrex.c`文件中, `ivm_doi_init_ProWritable`中的`flag`参数默认为0，只有当使用云API修改物模型时，`Method`参数配置为`report`时，需要将对应物模型的`flag`参数手动设置为1，其他情况均不用修改

### ivm_lock

**功能描述**  
物模型互斥加锁，在上报物模型前调用。

**函数原型**  

```
void ivm_lock(void);
```

**参数说明**  
| 参数名称 | 类型 | 描述 | 输入/输出 |
| -------- | ---- | ---- | --------- |
| -        | -    | -    | -         |

**返回值**  
| 返回值 | 描述 |
| ------ | ---- |
| -      | -    |

### ivm_unlock

**功能描述**  
物模型互斥解锁，在上报物模型后调用。

**函数原型**  

```
void ivm_unlock(void);
```

**参数说明**  
| 参数名称 | 类型 | 描述 | 输入/输出 |
| -------- | ---- | ---- | --------- |
| -        | -    | -    | -         |

**返回值**  
| 返回值 | 描述 |
| ------ | ---- |
| -      | -    |

### iv_dm_env_init_cb

**功能描述**  
物模型初始化参数函数回调

**函数原型**  

```
int (*iv_dm_env_init_cb)(void);
```

**参数说明**  
| 参数名称 | 类型 | 描述 | 输入/输出 |
| -------- | ---- | ---- | --------- |
| -        | -    | -    | -         |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

### ivm_property_report_cb

**功能描述**  
用户上报属性回调

**函数原型**  

```
int (*ivm_property_report_cb)(void *param, iv_dm_report_result_e result_code);
```

**参数说明**  
| 参数名称    | 类型                  | 描述     | 输入/输出 |
| ----------- | --------------------- | -------- | --------- |
| param       | void *                | 用户参数 | 输入      |
| result_code | iv_dm_report_result_e | 上报结果 | 输入      |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

### iv_msg_send_notice

**功能描述**  
用户上报消息通知，可用于小程序强提醒
不同消息ID对应的场景和含义，请参见产品文档
注意消息上报为异步方式，接口返回0表示入队成功，不代表实际发送结果

**函数原型**  

```
int iv_msg_send_notice(int msg_id, uint64_t utc_time_sec);
```

**参数说明**  
| 参数名称    | 类型                  | 描述     | 输入/输出 |
| ----------- | --------------------- | -------- | --------- |
| msg_id       | int                | 消息通知的ID | 输入      |
| utc_time_sec | uint64_t           | 消息发生时utc时间，单位秒 | 输入 |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码    |

### iv_msg_send_notice_ext

**功能描述**  
用户上报消息通知，支持json格式消息内容，可用于小程序强提醒
不同消息ID对应的场景和含义，请参见产品文档
注意消息上报为异步方式，接口返回0表示入队成功，不代表实际发送结果

**函数原型**  

```
int iv_msg_send_notice_ext(int msg_id, char *msg_data, int msg_data_len, uint64_t utc_time_sec);
```

**参数说明**  
| 参数名称    | 类型                  | 描述     | 输入/输出 |
| ----------- | --------------------| -------- | --------- |
| msg_id       | int                | 消息通知的ID | 输入      |
| msg_data     | char *             | 扩展消息内容，json格式 | 输入  |
| utc_time_sec | uint64_t           | 消息发生时utc时间，单位秒 | 输入 |

**返回值**  
| 返回值      | 描述                 |
| ----------- | -------------------- |
| IV_ERR_NONE | 成功                 |
| IV_ERR_*    | 失败，对应相应错误码 |

# 4. 数据结构

## 数据结构列表

**该模块提供以下数据结构**  

- iv_dm_report_result_e 物模型上报结果
- iv_dm_init_parm_s 物模型初始化参数结构体

## 数据结构描述

### iv_dm_report_result_e

**功能描述**  
物模型上报结果。

**结构原型**  

```
typedef enum
{
    REPORT_TIMEOUT  = -2,
    REPORT_REJECTED = -1,
    REPORT_ACCEPTED = 0  
} iv_dm_report_result_e;
```

**参数说明**  
| 成员名称        | 描述             | 取值 |
| --------------- | ---------------- | ---- |
| REPORT_TIMEOUT  | 物模型上报超时   | -2   |
| REPORT_REJECTED | 物模型上报被拒绝 | -1   |
| REPORT_ACCEPTED | 物模型上报成功   | 0    |

### iv_dm_init_parm_s

**功能描述**  
物模型初始化参数结构。

**结构原型**  

```
typedef struct iv_dm_init_parm_s {
    int (*iv_dm_env_init_cb)(void);
} iv_dm_init_parm_s;
```

**参数说明**  
| 成员名称          | 描述                     | 取值 |
| ----------------- | ------------------------ | ---- |
| iv_dm_env_init_cb | 物模型初始化参数函数回调 | 无   |

# 5. 注意事项

1. 不建议频繁调用模块相关接口
2. 初始化时SDK内部会获取最新物模型，直接读取 g_ivm_objs 即可，用户不必调用 iv_dm_property_sync 进行手动同步

# 6.物模型的创建和调试

首先登录物联网控制台，网址： [实例管理 - 物联网开发平台 - 控制台 (tencent.com)](https://console.cloud.tencent.com/iotexplorer/v2)

## 1.创建产品物模型

![](https://qcloudimg.tencent-cloud.cn/raw/129fd296fe02ddd220a2bd606f589638.png)

![](https://qcloudimg.tencent-cloud.cn/raw/ad5b361071b6795ab366ed6b7f8c7f97.png)

按照下图指示，选择“产品开发” -> “客户产品名称”，

如果没有产品，需要新建产品，然后再添加物模型。 新建产品请参考《腾讯Iot平台视频产品开发宝典》([企业微信文档](https://doc.weixin.qq.com/doc/w3_APwAzQZ-ACcaeVTWfNdSwaPs8e7KM?scode=AJEAIQdfAAoAuQHrnNAYUA4QbTAOw))

![](https://qcloudimg.tencent-cloud.cn/raw/670b1e7a3ee0eb80fbc129b0b0233641.png)

依次选择 物模型定义 -> 自定义功能 -> 添加自定义功能

![](https://qcloudimg.tencent-cloud.cn/raw/e096c90724e61c7dc15ef14ea7b77959.png)

## 2.物模型添加属性，事件，行为功能的示例

添加自定义功能的界面如下图，可以选择物模型的属性，事件，行为。

### 2.1下图是新增物模型属性的示例，

![](https://qcloudimg.tencent-cloud.cn/raw/4a2b6937af24f7ebdc28d2f1117a5264.png)

### 2.2下图是新增物模型事件的示例，参数数量可以有多个，参数类型也可以任意选择。

![](https://qcloudimg.tencent-cloud.cn/raw/497430798da38f5d0a3ffb07b0dfed6d.png)

### 2.3下图是新增物模型行为的示例，

调用参数数量可以有多个，参数类型也可以任意选择。调用参数至少要有一个参数，否则程序会运行异常。

返回参数数量可以有多个，参数类型也可以任意选择。返回参数至少要有一个参数，否则程序会运行异常。

![](https://qcloudimg.tencent-cloud.cn/raw/9d8746cd0a97985aa4dce9c61b4e87e9.png)

下图是添加完属性，事件，行为后的所有自定义功能。

![](https://qcloudimg.tencent-cloud.cn/raw/4f660cb9785d67a01e11869caf08572b.png)

## 2.4导入已有产品的物模型

除了上述添加物模型的方法，还可以基于已有产品的物模型json文件，导入物模型。在此基础上再添加新的物模型自定义功能。

![](https://qcloudimg.tencent-cloud.cn/raw/be89ba0d9fbe5382466dd962ffa4569d.png)

![](https://qcloudimg.tencent-cloud.cn/raw/f660a1e512fa540589edd0d45d66f55c.png)

## 2.5导出物模型

点击“查看物模型json”，可以导出物模型json文件。

![](https://qcloudimg.tencent-cloud.cn/raw/d44102fa0f9b18996e1c6ecd713d399a.png)

![](https://qcloudimg.tencent-cloud.cn/raw/acdb744db68903bbc7db189de175ac9a.png)

## 2.6生成物模型的C代码

物模型由用户声明好后导出相应的 json 文件, 使用工程目录下的 `tool/codegen.py` 脚本生成代码，执行如下命令： `./codegen.py -c <json文件>`

最终生成如下代码：

```
iv_usrex.c:用户物模型实现源文件,实现物模型初始化相关的代码,开发者不能修改这个文件
iv_usrex_func.c:用户物模型功能实现函数,实现相应 ProWritable 和 action 的回调
iv_usrex.h:用户物模型定义头文件,定义相关数据结构,开发者不能修改这个文件
```

在编译时编入工程，根据用户功能需要，在设备初始化阶段注册不同物模型处理函数，SDK 在相应模型触发时回调，用户亦可调用物模型接口主动上传物模型数据，对于设备来说，支持 ProWritable 类模型数据接收和发送, 支持 ProReadonly 和 Event 类模型数据发送， 支持 Action 模型数据接收。

**注意事项**

- codegen.py要求物模型json文件的编码格式是GBK，如果物模型json文件是UTF-8,可以修改codegen.py的open参数。见下图
- SDK sample例程的物模型代码iv_usrex.c, iv_usrex_func.c， iv_usrex.h路径在samples/samples/iot_video_demo/model/ 目录下。
- 物模型修改后，必须要重新使用codegen.py生成c代码，放到工程中重新编译代码。否则设备端和控制台的物模型不一致导致异常。
- 已量产产品请勿修改物模型。如果必须修改，设备要通过OTA升级，保证设备端和控制台物模型一致。

![](https://qcloudimg.tencent-cloud.cn/raw/02f7f077ddd0b8e9f354c6145590d829.png)

## 3.物模型的调试

物模型的调试，按照下图指导，点击“4设备开发” -> "客户的设备"

![](https://qcloudimg.tencent-cloud.cn/raw/485ebf1555e9d8accb1496bff3e1649b.png)

### 3.1物模型读写属性的调试

1. 勾选需要发送的属性并填写相关参数
2. 右侧【调试日志】窗口中勾选【显示响应报文】
3. 点击发送
4. 右侧【调试日志】窗口中会显示属性的下发和上报的数据和结果

![](https://qcloudimg.tencent-cloud.cn/raw/98db94d3c96a2fa17f58ca1dfb7c86a2.png)

### 3.2物模型行为的调试

1. 勾选需要下发的行为并填写相关参数
2. 右侧【调试日志】窗口中勾选【显示响应报文】
3. 点击发送
4. 右侧【调试日志】窗口中会显示对应日志，关于行为的详细日志可点击【设备云端日志】、【物模型日志】、【行为】，在此可查看行为的输入参数和输出参数

![](https://qcloudimg.tencent-cloud.cn/raw/fa241affa094dfea144652b80404f1f2.png)

### 3.3物模型事件的调试

物模型事件由设备端产生并上报控制台  
事件消息使用iv_dm_property_report()上报，详细使用方法请参考sdk例程  
设备端上报事件后，右侧【调试日志】窗口中会显示相关日志  

# 7. 示例代码
使用SDK例程前，请先在控制台导入sdk内附带的物模型json文件。此物模型例程仅用于展示属性、事件、行为的使用方法，请根据实际业务需求修改并使用物模型。

## 1. 物模型初始化

```
    int rc = 0;
    iv_dm_init_parm_s stInitParm;
    stInitParm.iv_dm_env_init_cb = ivm_env_init;
    rc                           = iv_dm_init(&stInitParm);
    if (rc < 0) {
        Log_e("model init failed!");
        return rc;
    }
```

## 2. 物模型去初始化

```
iv_dm_exit();
```

## 3. 属性和事件的上报

```
static struct dm_test_struct st_dm_test = {0};
static int dm_index = 0;

while(1){

    if(dm_index == 0) { /* 只读属性的上报示例 */
            /* st_dm_test.ro_battery_level在控制台设置的最大值是100，所以要取余*/
            st_dm_test.ro_battery_level = (time(NULL)) % 100;
            /* 只读属性必须用ivm_ProReadonly_xxx进行操作 */
            /* 此宏定义会完成属性的赋值以及上报的操作 */
            ivm_ProReadonly_setInt(battery_level, st_dm_test.ro_battery_level);

            DM_USER_DEBUG("dm_test_task set battery_level:%d", st_dm_test.ro_battery_level);
     } else if(dm_index == 1) {  /* 读写属性的上报示例 */
            st_dm_test.rw_lightness = (int)st_dm_test.rw_lightness % 100;
            st_dm_test.rw_lightness += 3.5;
            /* 读写属性必须用ivm_ProWritable_xxx进行操作 */
            /* 此宏定义会完成属性的赋值以及上报的操作 */
            ivm_ProWritable_setInt(scr_lightness, st_dm_test.rw_lightness);

            DM_USER_DEBUG("dm_test_task set scr_lightness:%f", st_dm_test.rw_lightness);
     } else if(dm_index == 2) { /* 读写属性的上报示例，增加了上报完成后的回调函数 */

            sprintf(st_dm_test.rw_welcom_str, "welcom_str:%ld", time(NULL));

            /* ivm_ProWritable_xxx 适合操作单个属性，对于更复杂的属性可以用以下方式进行上报 */
            ivm_lock();
            strncpy(g_ivm_objs.ProWritable.m_welcome_word, st_dm_test.rw_welcom_str, 2048);
            /* 在这里设置属性上报完成后的回调函数。 */
            iv_dm_property_report("welcome_word", ivm_welcom_report_cb, &st_dm_test);
            ivm_unlock();

            DM_USER_DEBUG("dm_test_task set welcome_word:%s, param:%p", st_dm_test.rw_welcom_str, &st_dm_test);
     } else if(dm_index == 3) {  /* 物模型事件的上报示例，*/
            user_event_attr_t ins_event_attr = {0};
            /* 用户业务检测是否有访客，ret==1,有访客，上报事件 */
            /* ret==1,有访客，上报事件 */
            /* ret==0,没有访客，无需上报事件 */ 
            int ret = detect_vistor(&ins_event_attr);
            if(ret == 1) {
                ivm_lock();
                strncpy(g_ivm_objs.Event.m_vistor_event.m_name, ins_event_attr.name, 40);
                g_ivm_objs.Event.m_vistor_event.m_hight      = ins_event_attr.height; 
                g_ivm_objs.Event.m_vistor_event.m_person_num = ins_event_attr.num;
                iv_dm_event_report("vistor_event");
                ivm_unlock();
            }
      }

      dm_index++;
      dm_index = dm_index % 4;
      HAL_DelayMs(10 * 000);
 }

/* 注意回调函数中禁止再次调用sdk提供的物模型的函数，
   比如ivm_ProWritable_setInt(),iv_dm_event_report()等函数， */
int ivm_welcom_report_cb(void *param, iv_dm_report_result_e result_code)
{
    struct dm_test_struct *pst_dm_test = (struct dm_test_struct *)param;
    DM_USER_DEBUG("welcom_report_cb param:%p", param);

    if(pst_dm_test) {
        DM_USER_DEBUG("welcom_report_cb now battery_level:%d", pst_dm_test->ro_battery_level);
        DM_USER_DEBUG("welcom_report_cb now scr_lightness:%f", pst_dm_test->rw_lightness);
        DM_USER_DEBUG("welcom_report_cb now welcome_word:%s", pst_dm_test->rw_welcom_str);

    }
    DM_USER_DEBUG("rockyshen welcom word report res:%d", result_code);
}
```



**注意事项**

- 注意物模型的回调函数中禁止再次调用sdk提供的物模型的函数。比如ivm_ProWritable_setInt(),iv_dm_event_report()等函数，